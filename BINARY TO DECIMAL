// Function 2: Binary to Decimal
int binToDec(string bin) {
    int dec = 0, power = 1;
    for(int i = bin.length()-1; i >= 0; i--) {
        if(bin[i] == '1') dec += power;
        power *= 2;
    }
    return dec;
}

