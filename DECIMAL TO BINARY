#include <iostream>
#include <string>
#include <cstdlib>
#include <ctime>
using namespace std;

// Function 1: Decimal to Binary
string decToBin(int n) {
    if(n == 0) return "0";
    string bin = "";
    while(n > 0) {
        bin = to_string(n % 2) + bin;
        n /= 2;
    }
    return bin;
}
